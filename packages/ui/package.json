{
  "name": "@cosmichub/ui",
  "version": "1.0.0",
  "description": "Shared UI components for CosmicHub applications",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'Skipping UI build - apps use direct TypeScript imports'", 
    "build:packages": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "files": [
    "dist",
    "src"
  ]
}
