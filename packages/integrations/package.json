{
  "name": "@cosmichub/integrations",
  "version": "1.0.0",
  "description": "Cross-app integrations for CosmicHub applications",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'Skipping integrations build - apps use direct TypeScript imports'",
    "build:packages": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@stripe/stripe-js": "^2.4.0",
    "zustand": "^4.4.0"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "files": [
    "dist",
    "src"
  ]
}
